Configuration:
  status: warn

  Appenders:
    Console:
      name: Console
      target: SYSTEM_OUT
      PatternLayout:
        pattern: "%d{HH:mm:ss} %-5level %logger{1.} - %msg%n"

  Loggers:
    Root:
      level: info
      AppenderRef:
        - ref: Console